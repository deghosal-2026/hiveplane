"""Rule lifecycle package.

Contains the stored, recomputed per-rule specificity scorer, the automated
retirement policy engine, supersession-chain helpers, and auto-promotion
tuning — the components that turn the rule store into a self-maintaining
asset (#512).
"""
