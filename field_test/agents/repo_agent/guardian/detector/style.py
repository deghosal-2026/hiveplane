"""Style-based AI code detection (Phase 2).
This module is a stub in Phase 1 and returns empty results.
"""
from guardian.detector.models import DetectionResult


def analyze_by_style(
    diff: str,
    model_endpoint: str | None = None,
    api_key: str | None = None,
) -> list[DetectionResult]:
    return []
